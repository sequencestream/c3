{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum surface the local startup screen needs: core events (startup progress / failure) plus the shell's own retry and quit commands. Scoped to the `splash` window ONLY — the `main` window renders the sidecar's remote SPA and is deliberately absent from every capability, so that content has no plugin permissions at all and in particular no shell execution.",
  "windows": ["splash"],
  "permissions": ["core:default"]
}
