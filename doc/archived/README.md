# Archived

废弃的文档与 domain 迁移到这里,保留原有结构,并在头部添加说明,
解释何时、为何废弃以及被什么取代。

- Domains:归档整个 `<domain>/` 目录;更新 group 概览以重定向。
- ADR **不**归档到这里 —— 被取代的 ADR 保留在
  `architecture/adr/deprecated/` 下(永不删除)。

初始化时为空(2026-05-29)。
